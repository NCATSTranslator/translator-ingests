name: 'human_phenotype_ontology_annotations'
metadata: !include './metadata.yaml'

readers:
  disease_to_phenotype:
    # TODO: is the 'name' tag redundant here, given the 'disease_to_phenotype' yaml key above?
    name: 'disease_to_phenotype'
    files:
      - 'data/hpoa/phenotype.hpoa'
    header_mode: 4
    format: 'csv'
    delimiter: '\t'
    # For a csv/tsv file, list expected columns
    columns:
      - 'database_id'
      - 'disease_name'
      - 'qualifier'
      - 'hpo_id'
      - 'reference'
      - 'evidence'
      - 'onset'
      - 'frequency'
      - 'sex'
      - 'modifier'
      - 'aspect'
      - 'biocuration'
  gene_to_disease:
    # TODO: is the 'name' tag redundant here, given the 'gene_to_disease' yaml key above?
    name: 'gene_to_disease'
    files:
      - './data/hpoa/genes_to_disease.txt'
    format: 'csv'
    delimiter: '\t'
    columns:
      - 'ncbi_gene_id'
      - 'gene_symbol'
      - 'association_type'
      - 'disease_id'
      - 'source'
  gene_to_phenotype:
    # TODO: is the 'name' tag redundant here, given the 'gene_to_phenotype' yaml key above?
    name: "gene_to_phenotype"
    depends_on:
      - 'src/translator_ingest/mappings/mondo/mondo_sssom_config.yaml'
    format: 'csv'
    delimiter: '\t'
    columns:
      - 'ncbi_gene_id'
      - 'gene_symbol'
      - 'hpo_id'
      - 'hpo_name'
      - 'frequency'
      - 'disease_id'
      - 'publications'
      - 'gene_to_disease_association_types'
    filters:
      - inclusion: 'include'
        column: 'gene_to_disease_association_types'
        filter_code: 'in'
        value:
          - 'MENDELIAN'
