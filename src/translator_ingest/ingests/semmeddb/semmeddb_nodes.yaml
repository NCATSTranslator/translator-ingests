name: semmeddb_nodes

reader:
  format: json
  files:
    # placeholder; prepare_data opens the actual jsonl
    - ../../../../data/semmedb/semmeddb_edges.jsonl

transform:
  # no extra fields required


