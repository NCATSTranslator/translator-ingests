# Files are saved as `local_name` which must match the file name used in the main ingest yaml
## updates 1-2 times a year since 2021
## data releases in Github repo: pull latest version tag from Github API (see .py get_latest_version)
---
- url: https://github.com/dgidb/dgidb-data/releases/download/{version}/interactions.tsv
  local_name: interactions.tsv