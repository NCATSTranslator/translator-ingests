# Specify the files that need to be downloaded for this ingest
# The download.yaml file format is defined and used by the KGHub Downloader to download files.
# https://monarch-initiative.github.io/kghub-downloader/
#
# Files are saved as `local_name` which must match the file names used in the main ingest yaml.
---
- url: https://example_url.io/Example_Source_File.tsv.gz
  local_name: Example_Saved_File.tsv.gz